/** Same tokens as src/styles.css — the share cards cannot read CSS variables. */
export const ogTheme = {
  colors: {
    bg: "#110c12",
    ink: "#f4eef2",
    muted: "#a8949c",
    accent: "#f0e6ea",
    track: "#2e242e",
    fill: "#8a3d9b",
    glans: "#c45a8a",
    danger: "#c45c4a",
  },
  meter: {
    minPx: 18,
    baseHalo: true,
  },
} as const;
